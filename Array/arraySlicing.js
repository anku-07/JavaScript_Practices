/*
Qn : Array Slicing
From the array colors = ['red', 'green', 'blue', 'yellow', 'orange'], extract a new array called subsetColors that contains only the elements 'blue', 'yellow', and 'orange'.
*/
let colors = ['red', 'green', 'blue', 'yellow', 'orange'];
let subsetColors = colors.slice(2);
console.log(subsetColors)