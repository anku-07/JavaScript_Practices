/*
Generate the multiplication table for 5 using a loop, printing each equation (e.g., 5 x 1 = 5, 5 x 2 = 10, etc.) up to 10.
*/
let num = parseInt(prompt("Enter a number : "));
for(let i = 1; i <= 10 ; i++){
    console.log(i*10);
}