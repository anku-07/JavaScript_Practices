//Sum of two number
const sum = (a,b) => {
    console.log(a+b);
};
// cube 
const cube = (n) => { // we can pass an argument without perethiases for example cube = n => {definition}
    return n*n*n;
}
//print Hello
const hello = () => { // when no one argument has not exist in an function so we must use perethiases for example hello =  => is an in valid syntax.
    console.log("Hello world!");
}
