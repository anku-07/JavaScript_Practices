/*
a. Declare an array colors with the names of three colors. Print the second color in the array.
b. Add a new color to the colors array and print the updated array.
*/
let colors = ['Red','Blue','Green'];
console.log(colors[1]);
colors.push('White');
console.log(colors);