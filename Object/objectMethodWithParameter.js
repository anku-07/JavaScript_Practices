//Create an object called person with a method greet that takes a name parameter and logs a greeting message. Test the method with different names.

const person = {
    greet(name){
        console.log("Hello " + name + "!");
    }
}