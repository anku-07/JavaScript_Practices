//Create an object called calculator with methods for addition, subtraction, multiplication, and division. Each method should take two parameters and perform the respective operation. Test each method with appropriate arguments and log the results.

const calculetor = {
    add : function(a,b){
        return a + b;
    },
    sub(a,b){
        return a - b;
    },
    mul(a,b){
        return a * b;
    },
    div(a,b){
        return a / b;
    }
}